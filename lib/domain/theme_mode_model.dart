import 'package:flutter/material.dart';

enum AppThemeMode { system, light, dark }

final appThemeModeData = {
  AppThemeMode.system: ThemeMode.system,
  AppThemeMode.light: ThemeMode.light,
  AppThemeMode.dark: ThemeMode.dark
};
